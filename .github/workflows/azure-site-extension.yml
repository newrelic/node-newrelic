name: Azure Site Extension

on:
  workflow_dispatch:

env:
  # Enable versioned runner quiet mode to make CI output easier to read:
  # OUTPUT_MODE: quiet
  SPEC_FILE_TEMPLATE: 'NewRelic.Azure.WebSites.Extension.NodeAgent.nuspec'

jobs:
  setup_tools:
    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v4
    - name: Setup dotnet '6.0.x'
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: '6.0.x'
    - name: Display dotnet version
      run: dotnet --version
    - name: Display NuGet version
      run: nuget ?

  create_extension_bundle:
    runs-on: windows-latest
    needs:
      - setup_tools

    strategy:
      matrix:
        node-version: ['20.x'] # Node 22 isn't yet available on Azure

    steps:
      - uses: actions/checkout@v4
      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}

      - name: Find agent version
        run: |
          $env:npm_agent_version = npm view newrelic version 
          echo "AGENT_VERSION=$env:npm_agent_version" | Out-File -FilePath $env:GITHUB_ENV -Append
      - name: Set package filename
        run: echo "PACKAGE_FILENAME=NewRelic.Azure.WebSites.Extension.NodeAgent.${{env.AGENT_VERSION}}.${{ matrix.node-version }}" | Out-File -FilePath $env:GITHUB_ENV -Append
      - name: Verify environment vars # because we can't access GH env vars until the next step
        run: |
          echo "Agent version: ${{ env.AGENT_VERSION }}"
          echo "Package filename: ${{ env.PACKAGE_FILENAME }}"
      - name: Install agent
        run: |
          cd "cloud-tooling/azure-site-extension/Content"
          npm i newrelic@${{ env.AGENT_VERSION }}

      - name: Configure package files
        run: |
          pwd
          (Get-Content cloud-tooling/azure-site-extension/${{ env.SPEC_FILE_TEMPLATE }}).Replace('{VERSION}', "${{ env.AGENT_VERSION }}").Replace('{NODE_VERSION}', "${{ matrix.node-version }}") | Set-Content cloud-tooling/azure-site-extension/${{ env.PACKAGE_FILENAME }}.nuspec
#          get-content ${{ env.SPEC_FILE_TEMPLATE }} | %{$_ -replace "{VERSION}","${{ env.AGENT_VERSION }}"} >> ${{NUSPEC_GENERATED}}

      - name: Create bundle
        run: |
          pwd
          cd cloud-tooling/azure-site-extension/
          nuget pack "${{ env.PACKAGE_FILENAME }}.nuspec"
          echo "Going to list directory contents"
          Get-ChildItem -Path .\*.* -Recurse -Force

#      - name: Archive package for verification
#        uses: actions/upload-artifact@v4
#        with:
#          name: azure-site-extension-test-${{ env.PACKAGE_FILENAME }}
#          path: cloud-tooling/azure-site-extension/.*

#      - name: publish_package
#        run: |
#          NUGET_API_KEY=$1
#          NUGET_SOURCE=$2
#          dotnet nuget push "NewRelic.Azure.WebSites.Extension.NodeAgent.${VERSION}.nupkg" --api-key ${NUGET_API_KEY} --source ${NUGET_SOURCE}
