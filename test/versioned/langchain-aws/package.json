{
  "name": "langchain-aws-bedrock-tests",
  "targets": [
    {
      "name": "@langchain/core",
      "minSupported": "0.1.17",
      "minAgentVersion": "11.13.0"
    }
  ],
  "version": "0.0.0",
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "tests": [
    {
      "engines": {
        "node": ">=20"
      },
       "comment": "because @langchain/core and @langchain/aws need to be the same major version, we will only test >=1.0.0 for both of them",
      "dependencies": {
        "@langchain/aws": ">=1.0.0",
        "@langchain/core": ">=1.0.0",
        "@langchain/community": ">=1.0.0",
        "@elastic/elasticsearch": "8.13.1"
      },
      "files": [
        "runnables.test.js",
        "runnables-streaming.test.js",
        "vectorstore.test.js"
      ]
    }
  ]
}