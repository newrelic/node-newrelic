'use strict'

const Logger = require('test-logger')
const Human = require('human')

class Person extends Human {
  #log

  constructor(logStream) {
    super(logStream)
    this.#log = new Logger(logStream)

    this.#log.info('person constructed')
  }

  get isPerson() {
    return true
  }
}

module.exports = Person
