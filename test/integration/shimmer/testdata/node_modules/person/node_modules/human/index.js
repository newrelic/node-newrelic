'use strict'

const Logger = require('test-logger')

class Human {
  #log

  constructor(logStream) {
    this.#log = new Logger(logStream)
    this.#log.info('human constructed')
  }

  get isHuman() {
    return true
  }
}

module.exports = Human
