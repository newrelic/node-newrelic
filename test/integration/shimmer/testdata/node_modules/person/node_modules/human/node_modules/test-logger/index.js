'use strict'

class Logger {
  #stream = process.stdout

  constructor(stream) {
    if (stream) {
      this.#stream = stream
    }
  }

  info(msg) {
    this.#stream.write(msg + "\n")
  }
}

module.exports = Logger
