{
  "id": "sql_params",
  "description": "compressed and encoded query parameters",
  "type": "object",
  "minProperties": 1,
  "properties": {
    "agent_run_id" : {
      "description": "this is where the run ID goes for SQL traces, instead of the top level",
      "type": "number",
      "mod": 1,
      "required": true
    }
  }
}
